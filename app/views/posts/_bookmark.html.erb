<%= turbo_frame_tag dom_id(post, :bookmark) do %>
  <%= link_to bookmarks_path(post_id: post.id), class: 'float-right', style: 'text-decoration:none', data: { "turbo-method": :post }, remote: true do %>
    <i class="bi bi-suit-heart"></i><%= post.bookmarks.count %>いいね！
  <% end %>
<% end %>
