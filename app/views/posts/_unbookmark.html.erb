<%= turbo_frame_tag dom_id(post, :bookmark) do %>
	<%= link_to bookmark_path(current_user.bookmarks.find_by(post_id: post.id)), id: "js-bookmark-button-for-post-#{post.id}", class: 'float-right', data: { turbo_method: :delete } do %>
		<i class="bi bi-bookmark-check-fill"></i>
	<% end %>
<% end %>
